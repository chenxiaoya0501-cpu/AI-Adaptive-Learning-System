# 学生端前端

路径：`apps/frontend/student`  
共享后端：`apps/backend`（API 前缀 `/api/v1/student`）

```bash
npm install
npm run dev
```

默认端口：http://localhost:5174

## 开发登录

**不需要模拟登录**。后端启动时会自动种子演示学生：

- 账号：`demo@local`
- 密码：`demo123`

也可在「注册」页自建学生账号（手机号或邮箱 + 密码）。
